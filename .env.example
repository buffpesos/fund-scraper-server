# Server Configuration
PORT=3001
NODE_ENV=production

# API Security
# Generate a secure random string for this
API_KEY=your_secret_api_key_here

# CORS Configuration
# URL of your Vercel app (e.g., https://your-app.vercel.app)
ALLOWED_ORIGIN=https://your-app.vercel.app

# Vercel Blob Storage
# Get this from: Vercel Dashboard → Storage → Blob → Your Store
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_XXXXXXXXXX_YYYYYYYY

# Cloudflare Tunnel (Optional - only if managing tunnel via environment)
# Most users will configure tunnel via config file instead
# TUNNEL_TOKEN=your_cloudflare_tunnel_token_here
